import 'package:flutter/material.dart';

class TaskListBar extends StatelessWidget {
  const TaskListBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
